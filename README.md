# My First Project

An End-to-End Retrieval-Augmented Generation (RAG) pipeline built with LangChain, Gemini Embedding (`gemini-embedding-001`), Gemini LLM (`gemini-2.5-flash`), and FAISS — loads a single PDF, chunks it, stores embeddings, and answers questions using semantic search. Runs entirely in the terminal with no UI.
